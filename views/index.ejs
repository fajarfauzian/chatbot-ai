<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI Chatbot</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container mt-4">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header bg-primary text-white">
                        <h2 class="text-center mb-0">🤖 AI Chatbot</h2>
                    </div>
                    <div class="card-body">
                    
                        <div id="chatContainer" class="chat-container mb-3" style="height: 400px; overflow-y: auto; border: 1px solid #ddd; padding: 15px; border-radius: 8px; background-color: #f8f9fa;">
                            <% if (chatHistory.length === 0) { %>
                                <div class="text-center text-muted mt-5">
                                    <p>Mulai percakapan dengan AI!</p>
                                    <small>Ketik pesan di bawah dan klik Kirim</small>
                                </div>
                            <% } else { %>
                                <% chatHistory.forEach(function(message) { %>
                                    <div class="message <%= message.role === 'user' ? 'user-message' : 'ai-message' %> mb-3">
                                        <div class="d-flex <%= message.role === 'user' ? 'justify-content-end' : 'justify-content-start' %>">
                                            <div class="message-bubble <%= message.role === 'user' ? 'bg-primary text-white' : 'bg-light' %> p-3 rounded" style="max-width: 70%;">
                                                <strong><%= message.role === 'user' ? 'Anda' : 'AI' %>:</strong>
                                                <div class="mt-1" style="white-space: pre-wrap;"><%= message.content %></div>
                                            </div>
                                        </div>
                                    </div>
                                <% }); %>
                            <% } %>
                        </div>

                        <form id="chatForm" action="/chat" method="POST">
                            <div class="input-group mb-2">
                                <input 
                                    type="text" 
                                    name="message" 
                                    id="messageInput" 
                                    class="form-control" 
                                    placeholder="Ketik pesan Anda..." 
                                    autocomplete="off"
                                >
                                <button type="submit" class="btn btn-primary" id="sendBtn">
                                    Kirim
                                </button>
                            </div>
                        </form>

                        <div class="text-center">
                            <form action="/clear" method="POST" style="display: inline;">
                                <button type="submit" class="btn btn-outline-danger btn-sm">
                                    🗑️ Hapus Riwayat
                                </button>
                            </form>
                        </div>

                        <div class="mt-3 p-2 bg-light rounded" style="font-size: 0.8rem;">
                            <strong>Debug:</strong>
                            <div>Total pesan: <%= chatHistory.length %></div>
                            <div id="debugInfo"></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        console.log('=== PAGE LOADED ===');
        
        window.addEventListener('DOMContentLoaded', function() {
            const chatContainer = document.getElementById('chatContainer');
            if (chatContainer) {
                chatContainer.scrollTop = chatContainer.scrollHeight;
            }
        });

        const form = document.getElementById('chatForm');
        const input = document.getElementById('messageInput');
        const sendBtn = document.getElementById('sendBtn');
        const debugInfo = document.getElementById('debugInfo');

        console.log('Form:', form);
        console.log('Form action:', form?.action);
        console.log('Form method:', form?.method);
        console.log('Input:', input);
        console.log('Input name:', input?.name);

        form.addEventListener('submit', function(e) {
            const message = input.value.trim();
            
            console.log('=== FORM SUBMIT ===');
            console.log('Message:', message);
            console.log('Form action:', form.action);
            console.log('Form method:', form.method);
            console.log('Input name:', input.name);
            console.log('Input value:', input.value);
            
            debugInfo.innerHTML = 'Submitting: ' + message;
            
            if (message === '') {
                e.preventDefault();
                alert('Silakan ketik pesan!');
                return false;
            }
            
            sendBtn.disabled = true;
            sendBtn.innerHTML = '<span class="spinner-border spinner-border-sm"></span> Mengirim...';
            input.disabled = true;
            
            debugInfo.innerHTML = 'Mengirim ke server...';
        });
    </script>
</body>
</html>